# Minimal but realistic config for integration tests

pod_spec:
  name: test-scaling-llms-e2e
  image_name: runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04
  gpu_type_id: NVIDIA A100-SXM4-80GB
  cloud_type: SECURE
  container_disk_in_gb: 20
  volume_in_gb: 40
  ports: "22/tcp"
  identity_file: ~/.ssh/runpod_key
  env:
    DATABASE_URL: ${DATABASE_URL}

provisioning:
  repo_dir: /workspace/repos/scaling-llms
  repo_url: https://github.com/vasilis-stylianou/scaling-llms.git
  repo_branch: main
  identity_file: ~/.ssh/runpod_key
  rclone_config_local: ~/.config/rclone/rclone.conf
  rclone_config_remote: /root/.config/rclone/rclone.conf
  create_jupyter_kernel: false
  kernel_name: scaling-llms
  kernel_display_name: "Python (scaling-llms)"
  poetry_install_args: []

job_spec:
  command: >
    poetry run python scripts/run_experiments.py
    tests/integration/run_experiments/conifgs/experiments/rclone_sync_test.yaml
  repo_dir: /workspace/repos/scaling-llms
  identity_file: ~/.ssh/runpod_key
  tmux_session_name: train
  log_path: /workspace/runs/train.log

workflow:
  reuse_if_exists: false
  timeout_s: 120
  poll_s: 5
  terminate_on_failure: false
  retry_policy:
    max_attempts: 5
    retry_base_s: 2.0
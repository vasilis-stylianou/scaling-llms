experiment_config: /Users/vasilis/Desktop/scaling-llms/src/scaling_llms/experiment_configs/tests/rclone_sync_runs.py
cleanup_after_sync: false   # delete local artifacts after each run syncs

registries:
  database_url_env_name: DATABASE_URL
  runs:
    # artifacts_root: /Users/vasilis/Desktop/artifacts
    artifacts_root: /workspace/artifacts/dev_runs
    table_name: dev_runs
    sync_hooks_type: rclone
    sync_hooks_args: 
      remote_rclone_name: r2
      remote_artifacts_root: scaling-llms/dev/runs
      global_args: ["--s3-no-check-bucket"]

  datasets:
    # artifacts_root: /Users/vasilis/Desktop/artifacts  
    artifacts_root: /workspace/artifacts/dev_datasets
    table_name: dev_datasets
    sync_hooks_type: rclone
    sync_hooks_args:
      remote_rclone_name: r2
      remote_artifacts_root: scaling-llms/dev/datasets
      global_args: ["--s3-no-check-bucket"]
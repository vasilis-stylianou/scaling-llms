experiment_config_module: scaling_llms.experiment_configs.test_run

registries:
  database_url_env_name: DATABASE_URL
  runs:
    artifacts_root: ~/Desktop/tests/runs/artifacts
    table_name: dev_runs
    sync_hooks_type:
    sync_hooks_args: {}
      

  datasets:
    artifacts_root: ~/Desktop/tests/datasets/artifacts
    table_name: dev_datasets
    sync_hooks_type:
    sync_hooks_args: {}